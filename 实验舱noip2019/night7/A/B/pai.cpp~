#include <bits/stdc++.h>
using namespace std;
int main() {
    int T=0;
    while(1) {
	system("test"),system("std"),system("");
	if(system("fc .out .out")) break;
	printf("AC%d\n",++T);
    }
    puts("WA");
    system("pause");
    return 0;
}
